[pytest]
pythonpath = src
asyncio_mode = auto
testpaths = tests
addopts = --cov=src --cov-report xml  --cov-fail-under=70 -v --color=yes -p nicegui.testing.plugin
main_file = src/main.py
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S